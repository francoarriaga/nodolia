<?php
// Conexión a la base de datos (Ajusta los detalles de conexión)
$conexion = mysqli_connect("localhost:3307", "root", "", "nodoliabd");
// Verificar la conexión
/*
if ($conexion) {
    echo"Exitoso";
}
        else{
            echo("No exitoso");
        }
    


// Obtener datos del formulario
//$usuario = $_POST['usuario'];
//$contraseña = $_POST['contraseña'];

// Consulta SQL para verificar las credenciales
//$sql = "SELECT * FROM usuarios WHERE usuario = '$usuario' AND contraseña = '$contraseña'";
//$resultado = $conexion->query($sql);

// Verificar si se encontró un usuario
//if ($resultado->num_rows > 0) {
 //   echo "<p class='mensaje-bienvenida'>¡Bienvenido, $usuario!</p>";
//} else {
//    echo "<p class='mensaje-error'>Credenciales incorrectas. Por favor, inténtalo de nuevo.</p>";
//}

// Cerrar la conexión
//$conexion->close();
*/

?>